type PokemonDataJson = {
  gen: number;
  shiny: Boolean; // if the pokemon is shiny
  mega: Boolean; // if the pokemon is a mega evolution
  legendary: Boolean;  // if the pokemon is a legendary
  female: Boolean;  // if the pokemon is female
  gigantamax: Boolean;  // if the pokemon is a gigantamax
  region: String; // the region the pokemon is from
  form: String; // the form of the pokemon
  animation: number; // what animation of the pokemon
  type: 'Pokemon'; // the type of the pokemon
};


    